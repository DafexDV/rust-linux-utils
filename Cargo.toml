[workspace]
resolver = "2"
members = [
    "grep",
    "cat",
    "wc",
    "head",
    "file-utils"
]

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT"

[workspace.dependencies]
clap = { version = "4.6.6", features = ["derive"] }
